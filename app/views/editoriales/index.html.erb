<h1>Listando editoriales</h1>

<table>
  <tr>
    <th>Codigo</th>
    <th>Nombre</th>
    <th>Disponible</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @editoriales.each do |editorial| %>
  <tr>
    <td><%= editorial.codigo %></td>
    <td><%= editorial.nombre %></td>
    <td><%= editorial.disponible %></td>
    <td><%= link_to 'Mostrar', editorial %></td>
    <td><%= link_to 'Editar', edit_editorial_path(editorial) %></td>
    <td><%= link_to 'Destroy', editorial, :confirm => 'Está seguro de eliminar?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nueva Editorial', new_editorial_path %>
