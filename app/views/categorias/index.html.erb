<h1>Listando categorias</h1>

<%= page_entries_info @categorias %>
<%= will_paginate @categorias %>

<table>
  <tr>
    <th>Nombre</th>
    <th>Disponible</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @categorias.each do |categoria| %>
  <tr>
    <td><%= categoria.nombre %></td>
    <td><%= categoria.disponible %></td>
    <td><%= link_to 'Mostrar', categoria %></td>
    <td><%= link_to 'Editar', edit_categoria_path(categoria) %></td>
    <td><%= link_to 'Eliminar', categoria, :confirm => 'Está seguro de eliminar?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nueva Categoria', new_categoria_path %>
